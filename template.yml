---
  AWSTemplateFormatVersion: "2010-09-09"
  Parameters:
    Domains:
      Description: "Comma-separated list of domain names"
      Type: "CommaDelimitedList"
  Resources:
    ACMCertificate:
      Type: "Custom::AcmCertificateRequest"
      Properties:
        ServiceToken: 'Your Lambda Function'
        Domains:
          Ref: Domains
